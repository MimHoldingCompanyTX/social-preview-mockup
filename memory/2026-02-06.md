# Durable Memory Snapshot - 2026-02-06

**Project State Stabilization:**
*   **Website Mockup (`design-mockup/`):** Restored to a stable, localized state including 3 services, refined story copy, and a functional local contact form. **Next step:** Public hosting deployment.
*   **Kanban Board (`ProjectTracker/`):** Repository Git setup finalized and committed. The board rendering is fixed by using static HTML content to bypass local JS caching issues, and it includes priority indicators. Drag/drop persistence is temporarily disabled for stability.
*   **Git Deployment:** Successfully committed files and set up the HTTPS remote for the `MimHoldingCompanyTX/Design_website` private repository using a securely stored Personal Access Token (PAT). **Next step:** User needs to store the PAT locally for Git to work automatically.
*   **Model Configuration:** Primary model set to `google/gemini-2.5-flash-lite-preview-09-2025` to prevent rate limiting. Local Ollama models (`llama3.2:3b`, `gemma3:4b`) are installed and configured for heartbeats.
*   **Voice/TTS:** TTS/Voice output is manually disabled due to previous rate-limiting issues, but the setup is complete.

**Key Decisions:**
*   Prioritized stability of the Kanban board over its dynamic functionality for now.
*   Shifted website deployment strategy to GitHub/Vercel/Netlify to resolve local file rendering bugs.
*   Maintained strict separation of project work from personal accounts via dedicated GitHub/Google/Apple IDs for this project.
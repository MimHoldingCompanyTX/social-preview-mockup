- Routing Policy Update: Primary model for coding/debug remains GPT-5. Gemini-flash is disabled by default to avoid quota loops; only used on explicit request. If any provider returns quota/429, auto-stop using that provider for the day and alert Chip; prefer GPT-5 or local models instead.

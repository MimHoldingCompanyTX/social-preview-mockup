## 2026-03-10 - Tuesday

### Git Remote Scan (Session Startup)
- **Scanned:** 5:03 AM CDT
- **Active repositories with remotes:**
  - `Design_Website_Live` (origin: https://github.com/MimHoldingCompanyTX/Design_Website_Live.git)
  - `Design_Website_Live copy` (duplicate)
  - `Design_Website_Live copy 2` (duplicate)
  - `Design_Website_Live-Backup` (duplicate)
  - `openclaw-config` (origin: https://github.com/MimHoldingCompanyTX/openclaw-config.git)
  - `ProjectTracker` (origin: https://MimHoldingCompanyTX:...@github.com/MimHoldingCompanyTX/ProjectTracker.git)
  - `design-mockup` (origin: https://MimHoldingCompanyTX:...@github.com/MimHoldingCompanyTX/Design_Website_Live.git)
  - Workspace root (`social-preview-mockup`) (origin: https://github.com/MimHoldingCompanyTX/social-preview-mockup.git)
- **Repositories without remotes:** `sheila_next`, `config-backup`
- **Note:** Multiple duplicate clones of Design_Website_Live present; consider cleanup.
### Client Portal Navigation Fixes (Morning)
- **Issue:** Navigation stack was broken - browser back button was skipping levels (Step → Client List instead of Step → Project → Client List)
- **Root cause:** `pushNavState` was pushing OLD state instead of NEW state before navigation
- **Fix:** Updated `handleProjectSelect` and `openStepView` to set new state first, then push new state
- **Testing:** Verified with OpenClaw browser automation; tested flow: Client List → Project → Step → Gallery, back button now works correctly at each level
- **Git:** Committed fixes (5f92315)

### Hydration Errors Fix (11:15 AM)
- **Issue:** Chrome extensions (password managers) adding `__gchrome_uniqueid` attributes causing server/client DOM mismatch
- **Symptoms:** React hydration errors in incognito on both desktop and mobile
- **Fix:** Added `suppressHydrationWarning` to all form elements (password inputs, file inputs, textareas) and `autoComplete="new-password"` + `data-1p-ignore` to prevent browser autofill interference
- **Files modified:** `app/portal/page.tsx`, `app/portal/portal-content.tsx`

### Mood Board Gallery Implementation (12:46 PM)
- **Request:** Implement same gallery/detail view for mood board phase as initial visit phase
- **Finding:** Mike Roselle's mood board already works (contains 15 PNG files directly, no iteration subfolders → triggers GalleryView automatically)
- **Rule:** Portal uses condition: if stepContents has folders with "iteration" in name, shows IterationView; else shows GalleryView
- **Decision:** Abandon iteration concept for mood boards; keep images in base folder to use GalleryView

### Favorite Toggle Implementation (1:17 PM) - IN PROGRESS
- **Goal:** User can favorite/unfavorite images in single image view (detailed gallery)
- **Location:** Button in bottom menu with iPhone-style heart icon
- **Implementation:**
  - Added favorites state with localStorage persistence
  - Created `toggleFavorite` and `isCurrentImageFavorited` functions
  - Added conditional button to bottom navigation (shows only when `viewingGalleryInline` is true)
- **Current status:** Build error due to JSX parsing issue at line 3370 (unterminated regexp literal)

### Favorite Toggle Implementation - FIXED (1:45 PM)
- **Build error:** JSX parsing error at line 3370 (unterminated regexp literal)
- **Root cause:** Incorrect nesting: `</>` fragment closing was inside `activeIterationSpecFile && (` conditional
- **Fix:** Swapped lines to place `)}` (close conditional) before `</>` (close fragment)
- **Result:** Build successful, dev server running
- **Git:** Committed fix (c79538a)

### Favorite Toggle Implementation - FIXED (1:49 PM)
- **Runtime error:** `useMemo is not defined` error after fixing JSX parsing
- **Root cause:** `useMemo` was not imported from React; only `useState, useEffect, useRef, useCallback` were imported
- **Fix:** Added `useMemo` to the import statement: `import { useState, useEffect, useRef, useCallback, useMemo } from 'react';`
- **Result:** Portal loads successfully; favorite toggle feature ready for testing
- **Git:** Committed import fix

### Browser Testing (1:49 PM)
- Verified portal loads at http://localhost:3000/portal
- Tested navigation: Client List → Project → Step → Workflow view
- Navigation stack working correctly (back button steps through levels)
- Favorite toggle button implementation complete and ready for manual testing

### Favorite Button Bug - Swiping Images Issue (2:35 PM)
- **Bug:** Favorite button showed "Unfavorite" (red heart) for all images after swiping, not just the favorited image
- **Root cause:** `InlineGalleryViewer` managed its own `currentIndex` state but didn't notify parent of changes; parent's `currentGalleryIndex` stayed at initial value (0)
- **Fix:** 
  - Added `onIndexChange` callback prop to `InlineGalleryViewer`
  - Added `handleGalleryIndexChange` in parent to update `currentGalleryIndex` when swiping
  - Connected the two via `onIndexChange={handleGalleryIndexChange}` in portal-content.tsx
- **Result:** Favorite button now correctly updates when swiping between images

### onIndexChange Error Fix (5:02 PM)
- **Error:** Runtime ReferenceError "onIndexChange is not defined" in moodboard phase
- **Cause:** `InlineGalleryViewer` component was missing `onIndexChange` in props destructuring
- **Fix:** Added `onIndexChange` to component props destructuring
- **Git:** Commit d03775a

### Favorites Filter Implementation (5:17 PM) - IN PROGRESS
- **Requirement:** Filter toggle in bottom menu to switch between "Favorites" and "All" views in moodboard gallery
- **Implementation:**
  - Added `showFavoritesOnly` state to track filter status
  - Added `toggleFavoritesFilter` function with console logging
  - Added filter button to bottom navigation (heart icon with dynamic text: "Favorites" when showing all, "All" when showing favorites)
  - Updated `GalleryView` component to accept `filterFavorites` and `favorites` props for filtering
  - Updated `IterationView` component to accept same props (implementation pending)
- **Status:** Filter button appears in UI; filtering logic partially implemented; needs completion of IterationView filter logic and parent-to-child prop passing

### Technical Decisions
- **Filter placement:** Bottom navigation (accessible across all step views)
- **Icon design:** Filled red heart when filter active (showing favorites), outline gold heart when inactive (showing all)
- **Text labels:** Dynamic: "Favorites" (filter inactive) vs "All" (filter active)
- **State persistence:** Not persisted across sessions (resets to "All" on page load)
- **Filter logic:** Uses existing `favorites` state object (ID → boolean mapping)

### Password Mask Visibility Fix (5:35 PM)
- **Issue:** Password dots in login page were too light (low contrast), making it hard for users to see if system was accepting input
- **User context:** Chip relies on voice translators and has visual impairment from stroke
- **Fix:** Added `text-gray-900` class to password input field for darker dots
- **File modified:** `app/portal/page.tsx`
- **Git commit:** 2e8cbb4

### Favorites Filter Implementation Complete (5:30 PM)
- **Requirement:** Filter toggle in bottom menu to switch between "Favorites" and "All" views in moodboard gallery
- **Implementation:**
  - `showFavoritesOnly` state + `toggleFavoritesFilter` function
  - Filter button in bottom navigation (heart icon with dynamic text)
  - GalleryView: Filters items based on favorites state
  - IterationView: Filters iterations based on favorited main images, shows "No favorite iterations" message when none
  - Active iteration adjusts automatically when filter changes
- **User experience:**
  - Button shows "Favorites" (outline heart) when filter inactive (showing all)
  - Button shows "All" (filled red heart) when filter active (showing favorites)
  - Iteration count updates to reflect filtered count
  - Empty state message when no favorites
- **Git commit:** ae6d591

### Decision: Abandon Iteration Concept Entirely (6:07 PM)
- **User direction:** Chip confirmed abandoning iteration concept for mood boards; will clean up Google Drive to remove confusion
- **Current portal behavior:** If no folders with "iteration" in name exist, portal defaults to GalleryView for direct image folders
- **Implications:** 
  - Iteration detection logic and spec-sheet UI remain in code but will not be triggered
  - Google Drive cleanup will ensure only GalleryView is used for mood boards
  - Favorites filter works for both gallery and (theoretical) iteration views
- **Technical note:** Code can be simplified later by removing iteration-related UI if desired, but currently dormant

### Fix JSON Files in Gallery and Favorites Issues (6:07 PM)
- **Issue 1:** JSON files (including favorites.json) were showing in mood board gallery
- **Fix:** Updated GalleryView filter to exclude `.json` and `.md` files; for mood board phases, only show image files (`mimeType?.startsWith('image/')`)
- **Issue 2:** Potential duplicate favorites.json files due to Google Drive allowing same filename
- **Fix:** Enhanced `saveFavoritesToFile` to detect and delete duplicate favorites.json files (keep first, delete others)
- **Verification:** Favorites are saved by file ID (not name) as confirmed in code
- **Files modified:** `app/portal/portal-content.tsx`
- **Git commit:** 914a08c

### HEIC File Display Fix (6:29 PM)
- **Issue:** HEIF (.heic) files show in gallery but don't display thumbnails
- **Root cause:** Browsers don't support HEIC format natively; Google Drive may serve HEIC directly
- **Fix:** Modified thumbnail and preview API routes to detect HEIC/HEIF/TIFF formats and use Google Drive's thumbnail service (converts to JPEG)
- **Implementation:**
  - `thumbnail/route.ts`: Checks for unsupported formats, uses thumbnailLink or Drive thumbnail service
  - `preview/route.ts`: Returns thumbnailUrl for unsupported formats instead of dataUrl
  - Added logging to track mimeType and thumbnail source
- **Gallery filtering:** JSON files (including favorites.json) now filtered out; mood boards show only images
- **Git commits:** ec29791, 40e1d11

### Production API 500 Error Fix (6:38 PM)
- **Issue:** "Error Loading Projects: Primary endpoint failed with status 500; Fallback failed with status 500" on live site (Vercel)
- **Root cause:** Google Drive authentication failing on Vercel due to missing service account credentials in serverless environment
- **Fix 1:** Updated `lib/google-drive.ts` to accept `GOOGLE_SERVICE_ACCOUNT_JSON` environment variable (JSON string) in addition to file path
- **Fix 2:** Updated `/portal/api/projects-gdrive` endpoint to use Google Drive API instead of GOG CLI (which doesn't exist on Vercel)
- **Both endpoints** now use service account authentication
- **Local testing:** Both endpoints return 200 with project data
- **Remaining action:** Set `GOOGLE_SERVICE_ACCOUNT_JSON` environment variable on Vercel with the service account JSON key
- **Git commits:** f3eccb7, 637ba9e

### Remove All GOG CLI Dependencies (7:27 PM)
- **Issue:** Production API 500 errors due to GOG CLI dependencies that don't exist on Vercel
- **Fix:** Updated all portal API endpoints to use Google Drive service account only, removing GOG CLI fallbacks:
  - `/api/projects` – service account only
  - `/api/project/steps` – service account only  
  - `/api/project/steps-create` – service account only
  - `/api/project/create` – service account only
- **Deleted:** All `fetchStepsWithGog`, `createFolderWithGog`, `fetchProjectsWithGog` functions
- **Simplified:** Endpoints now directly use `listFolders`, `createFolder`, `listFolderContents` from service account
- **Environment variable:** `GOOGLE_SERVICE_ACCOUNT_JSON` must be set on Vercel with the service account JSON key
- **Git commit:** 65966f3

# AgreementMaker Skill

This skill creates client agreements using a pure Python runner (`run.py`) that operates entirely within the main agent context, bypassing unreliable shell features and sub-agents.

**Usage:**
./AgreementMaker/run.py "Client Name" "YYYY-MM-DD" "Package Name"